{
	"name": "worker-alleato-fireflies-rag",
	"version": "2.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"deploy": "wrangler deploy",
		"deploy:production": "wrangler deploy --env production",
		"test": "vitest",
		"test:watch": "vitest watch",
		"cf-typegen": "wrangler types",
		"setup": "./scripts/setup.sh",
		"setup:kv": "wrangler kv:namespace create CACHE --preview false && wrangler kv:namespace create CACHE --preview",
		"setup:hyperdrive": "echo 'Run: npx wrangler hyperdrive create supabase-meetings --connection-string=\"YOUR_CONNECTION_STRING\"'",
		"secrets:list": "wrangler secret list",
		"tail": "wrangler tail",
		"lint": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/mcp-server-cloudflare": "^0.2.0",
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/pg": "^8.15.5",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.33.0"
	},
	"dependencies": {
		"@supabase/supabase-js": "^2.56.0",
		"postgres": "^3.4.5"
	}
}